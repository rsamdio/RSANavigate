rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Draft snapshots uploaded during walkthrough creation & authoring
    match /drafts/{demoId}/{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
      allow delete: if request.auth != null;
    }
  }
}
